// const policy_type = msg.data.pol_type;
// const policy_end_date = msg.data.pol_end_date;


const policy_type = "ประกันภัยรถยนต์";
const policy_end_date = 10/04/2022;

if (condition1) {
    // Statements
} else if (condition2) {
    // Statements
} else if (condition3) {
    // Statements
} else if (conditionN) {
    // Statements
} else {
    // Statements
}
 
if (policy_type === "ประกันภัยรถยนต์" || policy_type === "ประกันสุขภาพ"  && policy_end_date >= policy_end_date){
    return msg;
} else if (policy_type === "ประกันภัยรถยนต์" || policy_type === "ประกันสุขภาพ"  && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันภัยรถยนต์" || policy_type === "ประกันสุขภาพ"  && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันภัยรถยนต์" || policy_type === "ประกันสุขภาพ"  && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันสุขภาพ" && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันสุขภาพ" && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันสุขภาพ" && policy_end_date >= policy_end_date){
    return msg
} else if (policy_type === "ประกันสุขภาพ" && policy_end_date >= policy_end_date){
    return msg
}

console.log(policy_type);


if (date_sum > 60) {
    return msg
} else if (date_sum <= 60 && date_sum >= 0) {
    return [null,msg]
} else if (date_sum < 0 && date_sum >= -180) {
    return [null,null,msg]
} else if (date_sum < -180) {
    return [null,null,null,msg]
}